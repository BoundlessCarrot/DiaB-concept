/// Internal settings for the game
/// The radius of the ball (player)
pub const radius: f32 = 10.0;
/// The radius of the ball (player) as an integer
pub const radius_int: u32 = @as(u32, radius);

/// Width of the screen
pub const screenWidth = 1080;
/// Height of the screen
pub const screenHeight = 720;
